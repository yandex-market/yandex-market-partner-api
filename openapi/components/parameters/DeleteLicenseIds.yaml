description: Список идентификаторов лицензий для удаления.
name: ids
in: query
required: true
schema:
  type: array
  uniqueItems: true
  minItems: 1
  maxItems: 500
  items:
    type: integer
    format: int64
