description: >
  Конечная дата для фильтрации заказов по дате обновления (параметр
  `updatedAt`).


  Формат даты: `ДД-ММ-ГГГГ`.


  Между начальной (параметр `updatedAtDateFrom`) и конечной датой должно быть не
  больше 30 дней.


  Конечная дата не включается в интервал для фильтрации.
name: updatedAtDateTo
in: query
required: false
schema:
  type: string
  format: date
