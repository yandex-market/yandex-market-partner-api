description: Список идентификаторов лицензий.
name: ids
in: query
required: false
schema:
  type: array
  uniqueItems: true
  maxItems: 500
  minItems: 1
  items:
    type: integer
    format: int64
    minimum: 1
