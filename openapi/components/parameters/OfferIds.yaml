name: offer_id
in: query
x-transform: trim
required: false
description: Идентификаторы товаров в каталоге.
schema:
  type: array
  uniqueItems: true
  minItems: 1
  maxItems: 200
  items:
    $ref: ../schemas/ShopSku.yaml
