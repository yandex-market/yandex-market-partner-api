name: shop_sku
in: query
x-transform: trim
required: false
deprecated: true
description: |
  Идентификаторы товаров в каталоге.

  Используйте параметр `offer_id` вместо этого параметра.
schema:
  type: array
  uniqueItems: true
  minItems: 1
  maxItems: 200
  items:
    $ref: ../schemas/ShopSku.yaml
