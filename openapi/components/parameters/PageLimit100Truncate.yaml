name: limit
description: |
  {{ limit-truncate-param-description }}
in: query
required: false
x-transform: truncateLimit
schema:
  type: integer
  format: int32
  minimum: 1
  default: 50
  maximum: 100
