name: limit
description: Количество значений на одной странице.
in: query
required: false
x-transform: limit
x-limit-config:
  default: 100
  maximum: 200
  mode: strict
schema:
  type: integer
  format: int32
  minimum: 1
