name: limit
description: |
  {{ limit-truncate-param-description }}
in: query
required: false
x-transform: truncateLimit
schema:
  type: integer
  format: int32
  minimum: 50
  default: 250
  maximum: 500
