deprecated: true
description: |
  { % note warning "Параметр больше не используется" % }

  Передайте любое число, чтобы получился корректный :no-translate[URL].

  { % endnote % }

  Идентификатор грузоместа.
name: shipmentId
in: path
required: true
schema:
  type: integer
  format: int64
