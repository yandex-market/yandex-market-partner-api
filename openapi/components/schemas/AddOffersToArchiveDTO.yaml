description: Товары, которые не удалось поместить в архив.
type: object
properties:
  notArchivedOffers:
    description: Список товаров, которые не удалось поместить в архив.
    type: array
    nullable: true
    items:
      $ref: ./AddOffersToArchiveErrorDTO.yaml
