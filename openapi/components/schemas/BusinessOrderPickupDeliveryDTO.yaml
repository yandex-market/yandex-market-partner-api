description: Информация о доставке в пункт выдачи.
type: object
properties:
  address:
    $ref: ./BusinessOrderDeliveryAddressDTO.yaml
  region:
    $ref: ./RegionDTO.yaml
  logisticPointId:
    $ref: ./LogisticPointId.yaml
  outletCode:
    description: Идентификатор пункта самовывоза, присвоенный магазином.
    type: string
  outletStorageLimitDate:
    description: "Дата, до которой заказ будет храниться в пункте выдачи. Возвращается, когда заказ переходит в статус `PICKUP`.\n\nОдин раз дату можно поменять с помощью метода [PUT\_v2/campaigns/{campaignId}/orders/{orderId}/delivery/storage-limit](../../reference/orders/updateOrderStorageLimit.md).\n\nФормат даты: `ГГГГ-ММ-ДД`.\n"
    type: string
    format: date
