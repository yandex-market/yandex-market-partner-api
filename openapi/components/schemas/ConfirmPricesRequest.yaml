description: |
  Запрос на подтверждение цены.
type: object
required:
  - offerIds
properties:
  offerIds:
    description: Идентификаторы товаров, у которых подтверждается цена.
    type: array
    items:
      $ref: ./ShopSku.yaml
