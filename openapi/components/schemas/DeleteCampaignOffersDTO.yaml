description: >-
  Список товаров, которые не удалось удалить, потому что они не найдены или
  хранятся на складе Маркета.
type: object
properties:
  notDeletedOfferIds:
    description: Список SKU.
    type: array
    nullable: true
    minItems: 1
    items:
      $ref: ./ShopSku.yaml
