description: >-
  Список товаров, которые не удалось удалить, потому что они хранятся на складе
  Маркета.
type: object
properties:
  notDeletedOfferIds:
    description: Список SKU.
    type: array
    items:
      $ref: ./ShopSku.yaml
