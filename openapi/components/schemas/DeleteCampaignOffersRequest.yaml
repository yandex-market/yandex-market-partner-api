description: |
  Фильтрации удаляемых товаров по offerIds.
type: object
required:
  - offerIds
properties:
  offerIds:
    description: Идентификаторы товаров в каталоге.
    type: array
    minItems: 1
    maxItems: 500
    items:
      $ref: ./ShopSku.yaml
