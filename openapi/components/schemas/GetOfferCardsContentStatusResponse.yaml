description: Ответ со списком состояний товаров и пагинацией.
type: object
properties:
  paging:
    description: Ссылка на следующую страницу.
    $ref: ./ScrollingPagerDTO.yaml
  offerCards:
    description: Список состояний продуктов и их карточек.
    type: array
    items:
      $ref: ./OfferCardDTO.yaml
