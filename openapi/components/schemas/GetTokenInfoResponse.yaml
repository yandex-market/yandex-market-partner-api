description: Ответ на запрос получения информации о токене авторизации.
type: object
required:
  - result
allOf:
  - $ref: ./ApiResponse.yaml
  - properties:
      result:
        $ref: ./TokenDTO.yaml
