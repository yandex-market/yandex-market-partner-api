description: |
  Статус проверки лицензии:

  * :no-translate[`NEW`] — лицензия проверяется.
  * :no-translate[`SUCCESS`] — лицензия прошла проверку.
  * :no-translate[`FAIL`] — лицензия не прошла проверку.
  * :no-translate[`REVOKE`] — лицензия отозвана службой качества.
  * :no-translate[`DONT_WANT`] — не проверяется.
  * :no-translate[`FAIL_MANUAL`] — лицензия не прошла проверку службы качества.
type: string
enum:
  - NEW
  - SUCCESS
  - FAIL
  - REVOKE
  - DONT_WANT
  - FAIL_MANUAL
