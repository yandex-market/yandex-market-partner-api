description: |
  Тип акции:

  * :no-translate[`DIRECT_DISCOUNT`] — прямая скидка.

  * :no-translate[`BLUE_FLASH`] — флеш-акция.

  * :no-translate[`MARKET_PROMOCODE`] — скидка по промокоду.
type: string
enum:
  - DIRECT_DISCOUNT
  - BLUE_FLASH
  - MARKET_PROMOCODE
