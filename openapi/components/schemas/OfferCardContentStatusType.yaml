description: |
  Статус вычисления рейтинга карточки товара и рекомендаций:

  * :no-translate[`UPDATING`] — рейтинг обновляется.
  * :no-translate[`ACTUAL`] — рейтинг актуальный.
type: string
enum:
  - UPDATING
  - ACTUAL
