description: >
  Типы ошибок:


  * `UNKNOWN_CATEGORY` — указана неизвестная категория.

  * `CATEGORY_MISMATCH` — указанная категория не совпадает с категорией товара.

  * `EMPTY_MARKET_CATEGORY` — не указана маркетная категория при указании
  категорийных параметров.

  * `UNKNOWN_PARAMETER` — передана характеристика, отсутствующая среди
  характеристик категории.

  * `UNEXPECTED_BOOLEAN_VALUE` — вместо boolean-значения передано что-то другое.

  * `NUMBER_FORMAT` — передана строка, не обозначающая число, вместо числа.

  * `VALUE_BLANK` — передано пустое значение.

  * `INVALID_UNIT_ID` — передана единица измерения, недопустимая для
  характеристики.
type: string
enum:
  - UNKNOWN_CATEGORY
  - CATEGORY_MISMATCH
  - EMPTY_MARKET_CATEGORY
  - UNKNOWN_PARAMETER
  - UNEXPECTED_BOOLEAN_VALUE
  - NUMBER_FORMAT
  - VALUE_BLANK
  - INVALID_UNIT_ID
