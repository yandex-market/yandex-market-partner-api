description: Товар с информацией о новой цене на него.
type: object
properties:
  offerId:
    description: Идентификатор предложения.
    $ref: ./ShopSku.yaml
  id:
    description: |
      {% note alert "Это поле устарело" %}

      Не используйте его — это может привести к ошибкам.

      {% endnote %}

      Идентификатор предложения из прайс-листа.
    deprecated: true
    type: string
  feed:
    description: |
      {% note alert "Это поле устарело" %}

      Не используйте его — это может привести к ошибкам.

      {% endnote %}

      Информация о прайс-листе.
    deprecated: true
    $ref: ./OfferPriceFeedDTO.yaml
  price:
    $ref: ./PriceDTO.yaml
  marketSku:
    description: |
      {% note alert "Это поле устарело" %}

      Не используйте его — это может привести к ошибкам.

      {% endnote %}

      SKU на Маркете.
    deprecated: true
    $ref: ./MarketSku.yaml
  shopSku:
    description: |
      {% note alert "Это поле устарело" %}

      Не используйте его — это может привести к ошибкам.

      {% endnote %}
    deprecated: true
    $ref: ./ShopSku.yaml
