description: Информация о новой дате доставки заказа.
type: object
required:
  - toDate
properties:
  toDate:
    description: |
      Новая дата доставки заказа.

      Формат даты: `ГГГГ-ММ-ДД`.
    type: string
    format: date
