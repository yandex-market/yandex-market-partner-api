description: >
  Переданные вами для данной позиции коды маркировки. Возвращаются в двух
  вариантах — с криптохвостом и без.
type: object
properties:
  cis:
    description: Код «Честного знака» без криптохвоста.
    type: string
  cisFull:
    description: Код «Честного знака» с криптохвостом.
    type: string
