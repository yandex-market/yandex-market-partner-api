description: |
  Вид маркировки товара.

  * `CIS` — КИЗ, идентификатор "четный знак".

  * `UIN` — УИН, уникальный идентификационный номер.

  * `RNPT` — РНПТ, регистрационный номер партии товара.

  * `GTD` — номер ГТД, грузовой таможенной декларации.
type: string
enum:
  - CIS
  - UIN
  - RNPT
  - GTD
