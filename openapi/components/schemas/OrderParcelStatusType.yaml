description: |
  {% note alert %}

  Поле устарело и не рекомендуется к использованию.

  {% endnote %}

  Статус заказа в партнерской службе доставки.

  * `NEW` — новый.

  * `CREATED` — создан в службе доставки.

  * `READY_TO_SHIP` — посылка готова к передаче в службу доставки.

  * `ERROR` — ошибка создания заказа.

  * `UNKNOWN` — неизвестный статус.
type: string
deprecated: true
enum:
  - NEW
  - CREATED
  - READY_TO_SHIP
  - ERROR
  - UNKNOWN
