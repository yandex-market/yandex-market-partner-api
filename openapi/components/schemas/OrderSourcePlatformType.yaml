description: |
  Площадка-источник заказа:

  * `MARKET` — заказ, оформленный на Маркете.

  * `OTHER` — LaaS-заказ, созданный продавцом.
type: string
enum:
  - MARKET
  - OZON
  - WILDBERRIES
  - OTHER
