type: object
description: Информация об операции обновления статуса заказа.
allOf:
  - $ref: ./BaseOperationDTO.yaml
  - properties:
      errors:
        description: Список ошибок при обновления статуса заказа.
        type: array
        nullable: true
        minItems: 1
        items:
          $ref: ./OrderStatusUpdateErrorDTO.yaml
