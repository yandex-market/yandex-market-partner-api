description: Информация о комиссиях за заказ.
type: object
properties:
  type:
    description: Тип комиссии.
    $ref: ./OrdersStatsCommissionType.yaml
  actual:
    description: >
      Сумма комиссии в рублях, которая была выставлена в момент создания заказа
      и которую нужно оплатить.

      Точность — два знака после запятой.
    type: number
  predicted:
    description: >
      Сумма комиссии в рублях, которая была бы выставлена, если бы заказ был
      создан в момент формирования отчета по заказам.

      Точность — два знака после запятой.
    type: number
