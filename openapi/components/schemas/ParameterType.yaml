description: |
  Тип данных:

  * :no-translate[`TEXT`] — текст.
  * :no-translate[`ENUM`] — список возможных значений.
  * :no-translate[`BOOLEAN`] — :no-translate[`true`] или :no-translate[`false`].
  * :no-translate[`NUMERIC`] — число.
type: string
enum:
  - TEXT
  - ENUM
  - BOOLEAN
  - NUMERIC
