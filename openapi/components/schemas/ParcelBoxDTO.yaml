description: >-
  Элемент отображает одно грузовое место. Вложенные поля больше не используются,
  передавайте элемент пустым в запросах и не обращайте внимание на содержимое в
  ответах.
type: object
properties:
  id:
    description: >-
      Поле больше не используется. Не передавайте его в запросах и не обращайте
      на него внимания в ответах.
    type: integer
    format: int64
  fulfilmentId:
    description: >-
      Поле больше не используется. Не передавайте его в запросах и не обращайте
      на него внимания в ответах.
    type: string
    pattern: ^[\p{Alnum}- ]*$
