description: |
  Модель, по которой работает магазин:

  * :no-translate[`FBS`] — FBS или Экспресс.
  * :no-translate[`FBY`] — FBY.
  * :no-translate[`DBS`] — DBS.
type: string
enum:
  - FBS
  - FBY
  - DBS
