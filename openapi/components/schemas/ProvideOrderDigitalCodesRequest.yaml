description: Запрос на передачу ключей цифровых товаров.
type: object
required:
  - items
properties:
  items:
    description: >
      Список проданных ключей.


      Если в заказе есть несколько **одинаковых** товаров (например, несколько
      ключей к одной и той же подписке), передайте каждый в виде отдельного
      элемента массива. `id` у этих элементов должен быть один и тот же.
    type: array
    items:
      $ref: ./OrderDigitalItemDTO.yaml
