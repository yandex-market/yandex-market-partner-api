type: object
required:
  - items
properties:
  items:
    description: |
      Собственно список позиций, требующих маркировки.
    type: array
    items:
      $ref: ./OrderItemInstanceModificationCisDTO.yaml
