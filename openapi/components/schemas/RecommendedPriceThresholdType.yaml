description: >
  Фильтр по порогу рекомендованных цен на товары.


  * `OPTIMAL` — вывести товары, у которых цена попадает в оптимальный порог;

  * `AVERAGE` — вывести товары, у которых цена попадает в порог со средней
  ценой.
type: string
enum:
  - OPTIMAL
  - AVERAGE
