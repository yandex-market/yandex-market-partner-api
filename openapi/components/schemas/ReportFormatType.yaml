description: >
  Формат отчета:


  * :no-translate[`FILE`] — файл с электронной таблицей (:no-translate[XLSX]).

  * :no-translate[`CSV`] — :no-translate[ZIP]-архив с :no-translate[CSV]-файлами
  на каждый лист отчета.

  * :no-translate[`JSON`] — :no-translate[ZIP]-архив с
  :no-translate[JSON]-файлами на каждый лист отчета.
type: string
enum:
  - FILE
  - CSV
  - JSON
default: FILE
