description: >
  Формат отчета:


  * `FILE` — файл с электронной таблицей.

  * `CSV` — ZIP-архив с CSV-файлами на каждый лист отчета. Работает только для
  отчетов, где явно указана поддержка формата CSV.
type: string
enum:
  - FILE
  - CSV
