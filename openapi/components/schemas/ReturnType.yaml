description: |
  Тип заказа для фильтрации:

  * :no-translate[`UNREDEEMED`] — невыкуп.

  * :no-translate[`RETURN`] — возврат.

  Если не указывать, в ответе будут и невыкупы, и возвраты.
type: string
enum:
  - UNREDEEMED
  - RETURN
