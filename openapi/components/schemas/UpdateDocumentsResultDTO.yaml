type: object
description: Ошибки документов, которые не удалось обновить.
required:
  - errors
properties:
  errors:
    type: array
    minItems: 1
    maxItems: 100
    description: Ошибки обработки документов.
    items:
      $ref: ./UpdateDocumentErrorDTO.yaml
