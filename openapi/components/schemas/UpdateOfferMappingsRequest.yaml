type: object
required:
  - offerMappings
properties:
  offerMappings:
    description: Перечень товаров, которые нужно добавить или обновить.
    type: array
    minItems: 1
    maxItems: 500
    items:
      $ref: ./UpdateOfferMappingDTO.yaml
