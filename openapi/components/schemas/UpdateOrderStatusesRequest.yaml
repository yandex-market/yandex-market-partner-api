description: Список заказов.
type: object
required:
  - orders
properties:
  orders:
    description: Список заказов.
    type: array
    minItems: 1
    maxItems: 30
    items:
      $ref: ./OrderStateDTO.yaml
